<odoo>

    <data noupdate="0">
        <!--Email template -->
        <record id="spc_email_template_dispatching" model="mail.template">
            <field name="name">Dispatching: Send by email</field>
            <field name="model_id" ref="spc_dispatching_aziza.model_oscar_dispatch"/>
            <field name="email_from">${object.user_id.email_formatted |safe}</field>
            <field name="email_to">${(not object.partner_id and object.email_from or '') | safe}</field>
            <field name="partner_to">${object.partner_id.id or ''}</field>
            <field name="subject">Dispatching (${object.name})</field>
            <field name="body_html" type="html">
                <div style="margin: 0px; padding: 0px;">
                    <p style="margin: 0px; padding: 0px; font-size: 13px;">
                        <p>Bonjour,</p>
                        <p>Veuillez approuver ma demande de dispatching numéro  ${object.name}.</p>
                    </p>
                </div>
            </field>
        </record>

        <record id="spc_email_template_dispatching_cloture" model="mail.template">
            <field name="name">Dispatching Cloturée: Send by email</field>
            <field name="model_id" ref="spc_dispatching_aziza.model_oscar_dispatch"/>
            <field name="email_from">${object.user_id.email_formatted |safe}</field>
            <field name="email_to">${(not object.partner_id and object.email_from or '') | safe}</field>
            <field name="partner_to">${object.partner_id.id or ''}</field>
            <field name="subject">Dispatching Cloturée(${object.name})</field>
            <field name="body_html" type="html">
                <div style="margin: 0px; padding: 0px;">
                    <p style="margin: 0px; padding: 0px; font-size: 13px;">
                        <p>Bonjour,</p>
                        <p>La demande de dispatching numéro  ${object.name} est cloturée</p>
                    </p>
                </div>
            </field>
        </record>
    </data>
</odoo>
