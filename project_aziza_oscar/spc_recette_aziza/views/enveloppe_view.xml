<?xml version="1.0" encoding="UTF-8"?>

<odoo>
    <data>

        <record id="view_recette_envelope_form" model="ir.ui.view">
            <field name="name">oscar.recette.envelope.form</field>
            <field name="model">oscar.envelope</field>
            <field name="inherit_id" ref="spc_transfer_aziza.envelope_view_form"/>
            <field name="arch" type="xml">
                <xpath expr="/form/sheet//group[@name='pieces_jointes']/notebook" position="inside">
                        <page string="Journées" attrs="{'invisible': [('journee_content', '!=', True)]}" >
                            <field name="journee_content" invisible="1"/>
                            <field name="journee_ids" widget="many2many" domain="[('envelope_id','=',False)]"
                                   attrs="{'invisible': [('journee_content', '!=', True)]}" options="{'no_create': True}"/>
                        </page>
                </xpath>
            </field>
        </record>


    </data>
</odoo>
